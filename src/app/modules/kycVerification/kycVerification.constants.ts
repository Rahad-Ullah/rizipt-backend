export enum KycType {
    Identity = "identity",
    Academic = "academic",
    License = "license",
}

export enum KycStatus {
    Pending = "pending",
    InReview = "in_review",
    ActionRequired = "action_required",
    Approved = "approved",
    Rejected = "rejected",
}